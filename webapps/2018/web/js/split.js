function split() {
    var numbers = document.getElementById("numbers").split(" ");
    for(var i = 0; i < numbers.size(); i++) {
        if(!isNaN(numbers[i])) {
            var numbers [] = [];
            break;
        }
    }
    document.getElementById("numbers").innerHTML = numbers;
}